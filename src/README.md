# src/

Application code, one subfolder per ComplianceTrack module. Empty except for
placeholder READMEs until Unit 4, when Flask routes and views are added
here. Shared code (authentication, authorization, audit logging) will live
in a `shared/` folder introduced at that point.
