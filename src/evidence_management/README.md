# Evidence Management module

Scaffolded in Unit 3. Implementation begins in Unit 4 per the course
schedule; this folder is a placeholder so the repository layout matches the
module boundaries fixed in the system architecture (see `design/`).
